<% if @communities.size > 0 %>
<% @communities.each do |communitiy| %>

    <div>
        <h3>
            <%= link_to community.name, community %>
        </h3>
        <%= simple_format community.bio %>
    </div>

    <% end %>
<% else %>
<h4 class="text-center mt-5">No Communities</h4>
<% end %>