$("#post-comments").html("<%= escape_javascript(render partial: 'posts/comments', locals: { comments: @comments.order("id") }) %>")
$("#post-comment").text("<%= pluralize @post.total_comments, "comment" %>")
$("#new_comment").trigger("reset")